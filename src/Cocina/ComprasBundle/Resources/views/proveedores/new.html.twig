{% extends 'backend.html.twig' %}

{% block body %}
    <h1>Nuevo proveedor</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Crear" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('proveedores_index') }}">Volver a la lista</a>
        </li>
    </ul>
{% endblock %}
